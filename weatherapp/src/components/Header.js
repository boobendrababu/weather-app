import React from "react";
import { Menu } from "semantic-ui-react";

const Header = () => (
  <Menu secondary pointing>
    <div className="header item">Sample UI - Weather App</div>
  </Menu>
);

export default Header;
